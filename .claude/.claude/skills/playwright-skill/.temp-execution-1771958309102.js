const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch({ headless: false }); const page = await browser.newPage(); await page.setViewportSize({ width: 1920, height: 1080 }); await page.goto('http://localhost:8000/login', { waitUntil: 'networkidle' }); await page.fill('#email', 'mahirr@mahir.be'); await page.fill('#password', 'mahir123'); await page.click('button[type="submit"]'); await page.waitForTimeout(3000); await page.screenshot({ path: 'c:/xampp/www/knowledgemcp/tmp-dashboard.png', fullPage: true }); console.log('URL:', page.url()); await browser.close(); })();